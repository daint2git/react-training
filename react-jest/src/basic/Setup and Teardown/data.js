const initializeCityDatabase = () => {
  return ['ha noi', 'da nang', 'ho chi minh']
}

const initializePositionDatabase = () => {
  return ['QA', 'developer', 'tester']
}

export { initializeCityDatabase, initializePositionDatabase }
